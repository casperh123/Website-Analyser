@using WebsiteAnalyzer.Core.DataTransferObject
@using WebsiteAnalyzer.Web.Components.BrokenLink.Cards

@if (BrokenLinks.Count > 0)
{
    foreach (BrokenLinkDTO brokenLink in BrokenLinks)
    {
        <BrokenLinkCard Link="@brokenLink" />
    }
}

<AuthorizeView>
    
</AuthorizeView>

@code {
    [Parameter]
    public required IList<BrokenLinkDTO> BrokenLinks { get; set; }
}