@if (Visible)
{
    <RadzenAlert AlertStyle="AlertStyle.Danger" Text="@Message" bind-Visible="Visible"/>
}

@code {
    [Parameter] public required string Message { get; set; }

    private bool Visible => !string.IsNullOrEmpty(Message);
}